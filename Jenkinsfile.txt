pipeline {
    agent any
    stages {
        stage("Clean Up"){
            steps {
                deleteDir()
            }
        }

        stage("terraform init") {

            steps{

                sh ("terraform init")

            }

        }
        stage("Checkout") {
            steps{
                sh "git clone https://github.com/CameronR16/DevelopmentProject.git"
            }
        }

        stage("terraform Action") {

            steps {
                sh ("terraform apply --var-file "tfvars/dev.tfswars" --auto-approve")
            }
        }
    }

}
